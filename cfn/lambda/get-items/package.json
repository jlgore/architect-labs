{
  "name": "get-items-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to get items from DynamoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "*"
  }
} 