{
  "name": "process-streams-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to process DynamoDB streams",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "*"
  }
} 